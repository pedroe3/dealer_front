
export const isAuthenticated = state => {
    return state.isAuthenticated
}

export const userMe = state => {
    return state.user
}

export const auth = state => {
    return state
}

export const isSuperAdmin = state => {
    return state.isSuperAdmin
}
